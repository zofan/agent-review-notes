  <idea-plugin>
    <id>ai.agentreviewnotes</id>
    <name>Agent Review Notes</name>
    <vendor>0x9171</vendor>

    <description><![CDATA[
        Agent Review Notes stores local code and directory review comments without changing project files and includes a bundled AI-agent skill for reading and updating notes.
        <p>
            Плагин сохраняет локальные замечания к коду и структуре проекта, не изменяя исходные
            файлы и не отправляя данные в сеть.
        </p>
        <p>
            Плагин поставляется со встроенным skill <code>agent-review-notes</code> для AI-агента.
            Skill описывает безопасное чтение и изменение очереди замечаний и устанавливается в
            проект кнопкой <b>Install SKILL</b> в окне Agent Review.
        </p>
        <p>Плагин поддерживает:</p>
        <ul>
            <li>замечания на выделенный код, позицию каретки, файл или каталог;</li>
            <li>точные anchors со snapshot, контекстом и Git revision;</li>
            <li>привязку к Git repository и branch в multi-root проектах;</li>
            <li>просмотр в tool window и навигацию из gutter;</li>
            <li>фильтрацию по типу, изменение статуса, редактирование и удаление;</li>
            <li>безопасную перепривязку после изменения файла или явный статус needs_reanchor;</li>
            <li>открытый versioned JSON-контракт для интеграции с локальными AI-агентами.</li>
        </ul>
        <p>
            Заметки хранятся только в локальном каталоге проекта. Плагин не запускает AI-агентов
            самостоятельно и не добавляет служебные комментарии в production-код.
        </p>
        <p>
            Copyright © Andrey Loenov
        </p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>Git4Idea</depends>

    <actions>
        <action id="AgentReviewNotes.AddNote"
                class="ai.agentreviewnotes.action.AddReviewNoteAction"
                text="Add Review Note for AI Agent"
                description="Create a local review note for an AI agent">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt R"/>
        </action>
        <action id="AgentReviewNotes.AddDirectoryNote"
                class="ai.agentreviewnotes.action.AddDirectoryReviewNoteAction"
                text="Add Review Note to Directory"
                description="Create a local review note for the selected directory">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="first"/>
        </action>
        <action id="AgentReviewNotes.Help"
                class="ai.agentreviewnotes.action.ReviewNotesHelpAction"
                text="Agent Review Notes Help"
                description="Show usage instructions and tips for Agent Review Notes">
            <add-to-group group-id="HelpMenu" anchor="last"/>
        </action>
        <group id="AgentReviewNotes.KeymapGroup" text="Agent Review Notes" popup="false">
            <reference ref="AgentReviewNotes.AddNote"/>
            <reference ref="AgentReviewNotes.AddDirectoryNote"/>
        </group>
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Agent Review"
                    anchor="right"
                    factoryClass="ai.agentreviewnotes.ui.ReviewNotesToolWindowFactory"/>
        <postStartupActivity implementation="ai.agentreviewnotes.startup.ReviewNotesProjectActivity"/>
        <projectViewNodeDecorator implementation="ai.agentreviewnotes.projectview.ReviewNoteProjectViewDecorator"/>
    </extensions>
</idea-plugin>
